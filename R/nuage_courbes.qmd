---
title: "Nuage"
format: html
---

```{r}
chrono_exp <- function(data, var, exp_sel = NULL) {

  if (!is.null(exp_sel)) {
    data <- dplyr::filter(data, exp %in% exp_sel)
  }

  ggplot(data, aes(x = DATE_s, y = {{var}}, color = exp, group = exp)) +
    geom_line() +
    theme_minimal() +
    labs(x = "Date", y = deparse(substitute(var))) +
    theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
    scale_x_date(
      date_labels = "%d-%b",
      limits = as.Date(c("2000-08-15", "2001-08-15"))
    )
}
```



TEMP_T_Q

```{r}
fdobj <- data_acp_f(TEMP_T_Q, centre = F, plot = T)
pca <- pca.fd(fdobj, nharm = 5)
plot_indiv(pca,
           type_rend = "SY",
           var = "Montego",
           axe = c(1, 2),
           variable = "TEMP_T_Q")
chrono_exp(dt_meteo, "TEMP_T_Q", exp_sel = c("INRA13"))

```


